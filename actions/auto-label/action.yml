name: Auto Label
description: Labels PRs automatically based on changed files

inputs:
  sync-labels:
    description: Remove labels when matching files are reverted
    required: false
    default: 'true'

runs:
  using: composite
  steps:
    - uses: actions/labeler@v5
      with:
        sync-labels: ${{ inputs.sync-labels }}
